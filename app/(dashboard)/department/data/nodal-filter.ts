/** Faceted filter options — mirrors tasks `statuses` / `priorities` pattern */
export const nodalFilterOptions = [
	{ label: "Assigned", value: "assigned" },
	{ label: "Unassigned", value: "unassigned" },
] as const;
